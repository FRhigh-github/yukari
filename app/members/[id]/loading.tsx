// loading.tsx という名前でファイルを置くと、
// そのページのデータが届くまでの間、これが代わりに表示されます。
//
// この画面は、ホームから来たときは全画面のストーリー、
// プロフィールや設定から来たときは写真の一覧になります（MemberPosts）。
// 待ち画面はどちらから来たかを知れないので、どちらにも合う
// 「生成り色の無地」にしています。形のある骨組みを出すと、違う形から切り替わって目立つためです。

export default function Loading() {
  return <div className="absolute inset-0 z-40 bg-[#faf9f6]" />;
}
