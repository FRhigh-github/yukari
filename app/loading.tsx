// ▼ 何も描かない「待ち画面」です。
//
// 空なのに意味があります。
// loading.tsx が置いてあるページだけ、Next.js は
// 「先に骨組みだけ出して、中身は届きしだい流し込む」ことができます。
// これが無いと、押してから中身がDBから届くまで画面が動きません。
//
// 結び目のローダーを出すと、一瞬だけ見えて消える点滅になったので、
// 何も描かずにしてあります。中身が届いた瞬間に、そのまま置き換わります。
//
// 色は、ほとんどの画面の背景と同じ生成り色（#faf9f6）にしています。
// 前は真っ白で、画面を移るたびに一瞬だけ白く光って見えていました。

export default function Loading() {
  return <div className="h-full bg-[#faf9f6]" />;
}
